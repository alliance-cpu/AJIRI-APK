package com.example.ajiri.ui.theme

import androidx.compose.ui.graphics.Color

// Couleurs primaires de l'application
val Green700 = Color(0xFF388E3C) // Vert foncé pour le mode sombre
val Green500 = Color(0xFF4CAF50) // Vert standard pour le mode clair

// Couleurs de base pour l'interface
val Black = Color(0xFF000000) // Noir pour le texte sur fond clair
val White = Color(0xFFFFFFFF) // Blanc pour le fond de l'application

// Couleurs pour les erreurs
val ErrorRed = Color(0xFFB00020) // Rouge pour les messages d'erreur

